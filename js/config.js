// Set this to your n8n instance's webhook base URL once your workflow is
// active, e.g. "https://your-instance.app.n8n.cloud/webhook"
const N8N_BASE_URL = "https://n8n-ayr3.srv1861562.hstgr.cloud/webhook";

// Clerk publishable key, from the Clerk dashboard under API keys.
// It starts with pk_test_ or pk_live_ and is meant to be public, so it is safe
// to commit — the secret key is the one that must never leave your server.
// While this is empty the sign-in screen says so instead of hanging.
const CLERK_PUBLISHABLE_KEY = "";
